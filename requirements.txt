# 🦞 炒股龙虾系统 - Python 依赖
# 使用方式：pip3 install -r requirements.txt

# 核心依赖
requests>=2.28.0          # HTTP 请求库
pandas>=1.5.0             # 数据处理
numpy>=1.23.0             # 数值计算

# 可选依赖（高级功能）
# akshare>=1.9.0          # A 股数据接口（如需使用）
# redis>=4.5.0            # Redis 缓存（如需使用）

# 开发依赖（可选）
# pytest>=7.0.0           # 测试框架
# black>=22.0.0           # 代码格式化
